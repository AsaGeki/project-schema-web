import { Router } from 'express';

// Exemplo de como registrar rotas de módulos:
// import usersRouter from '@modules/users/infra/https/routes/users.routes';
// routes.use('/users', usersRouter);

const routes = Router();

export default routes;
